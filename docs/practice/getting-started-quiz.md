---
tags:
  - quiz
  - getting-started
---

# Getting Started Quiz

Test your understanding of the FLOWCAL data journey.

<div class="quiz-deck" data-color="blue">

<div class="qz-q" data-correct="1" data-explanation="Per the Security: Users and Groups section, FLOWCAL creates groups with different privilege levels and assigns users to the group matching the access they need.">
<p class="qz-question-text">How is access to FLOWCAL controlled?</p>
<span class="qz-opt">Every user shares a single admin account</span>
<span class="qz-opt">Users are assigned to groups that carry distinct privilege levels</span>
<span class="qz-opt">Access is granted purely by IP-address allowlisting</span>
<span class="qz-opt">Access is controlled per-ticket rather than per-user</span>
</div>

<div class="qz-q" data-correct="1" data-explanation="The End-to-End Data Flow section states that data failing validation closes on an exception, which must be resolved before the meter can be closed.">
<p class="qz-question-text">What happens when incoming meter data fails validation in FLOWCAL?</p>
<span class="qz-opt">It is silently discarded</span>
<span class="qz-opt">It closes on an exception that must be resolved</span>
<span class="qz-opt">It is automatically approved through PPA</span>
<span class="qz-opt">It is rolled up as-is into reports</span>
</div>

<div class="qz-q" data-correct="1" data-explanation="Per the End-to-End Data Flow section, changes to a closed meter require approval through the PPA workflow.">
<p class="qz-question-text">What is required to make a change (e.g. an equation change) to an already-closed meter?</p>
<span class="qz-opt">No approval is needed since the meter is already closed</span>
<span class="qz-opt">The change must go through PPA approval</span>
<span class="qz-opt">The meter must be deleted and recreated</span>
<span class="qz-opt">A new group must be created for the change</span>
</div>

</div>
